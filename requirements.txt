fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
boto3>=1.35.50
python-dotenv>=1.0.1
httpx>=0.27.2
structlog>=24.4.0
sqlalchemy>=2.0.0
asyncpg>=0.29.0
pypdf>=5.0.0
python-telegram-bot>=22.0
jinja2>=3.1.0
playwright>=1.49.0
# WEBP→JPEG conversion for HeyGen avatar-look previews sent to Telegram (app/tools/videos).
Pillow>=10.0.0
# Google Gemini "Nano Banana Pro" image model — personal-branding image generation
# (app/integrations/gemini_images). AI Studio / Generative Language API (not Vertex).
google-genai>=2.9.0
# .xlsx inventory exports (app/tools/inventory_export).
openpyxl>=3.1.0
h2>=4.0.0
# Verify Supabase user JWTs on /v1/chat (asymmetric ES256/RS256 via the project JWKS).
# The [crypto] extra pulls in `cryptography` for ECDSA/RSA signature verification.
pyjwt[crypto]>=2.9.0
